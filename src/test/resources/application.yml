spring:
  datasource:
    url: jdbc:h2:mem:leadmagnet;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  auth:
    secret: test-secret-that-is-long-enough
    token-hours: 1
  cors-origin: http://localhost:5173
  seed-demo-data: false
